.container-fluid
	.content
		.wrapper
			.proper-content
				.row-fluid
					.span2
						.well.sidebar-nav
							%ul.nav.nav-list
								- @arr.each do |page|
									%li
										%a{:href => "#", :class => page}
											= page
					.span9
						.row-fluid
							%pre{:id => "pattern"}
								Click any pattern to see its contents.
			.push
		.footer-wrapper
			%footer
				%div=haml :footer
%script{ :type => "text/javascript", :src =>"//cdnjs.cloudflare.com/ajax/libs/jquery/1.8.1/jquery.min.js"}
:javascript
	$("a").click(function () {
		$.get('patterns/' + $(this).attr("class"), function(data){
		$('#pattern').text(data);
		});
	});
